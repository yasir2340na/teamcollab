# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# Local MongoDB
MONGODB_URI="mongodb+srv://yasirali:Ahad12%40%40@teamcollab.wpao9dc.mongodb.net/?appName=teamcollabs";
# MongoDB Atlas (alternative)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/teamcollab?retryWrites=true&w=majority

# JWT Secret (IMPORTANT: Change this to a long, random string in production!)
# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_use_a_long_random_string

# AI API Keys (Optional - for AI task suggestions feature)
# Get your Mistral API key from: https://console.mistral.ai/api-keys
MISTRAL_API_KEY=your_mistral_api_key_here
COHERE_API_KEY=your_cohere_api_key_here

# CORS Configuration (comma-separated list of allowed origins)
# In development, you can use * or leave empty
# In production, specify your frontend URLs
# ALLOWED_ORIGINS=http://localhost:5173,https://yourdomain.com

# Rate Limiting (Optional - for future implementation)
# RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
# RATE_LIMIT_MAX_REQUESTS=100  # Max 100 requests per window

# Email Configuration (Optional - for future notifications feature)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=TeamCollab <noreply@teamcollab.com>

# Session Configuration
# SESSION_SECRET=your_session_secret_here

# File Upload Configuration (Optional - for future file attachments)
# MAX_FILE_SIZE=10485760  # 10MB in bytes
# ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,application/pdf

# Logging Configuration
# LOG_LEVEL=info  # Options: error, warn, info, debug
# LOG_FILE=logs/app.log

# Redis Configuration (Optional - for caching and session store)
# REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=your_redis_password

# Security Headers (Optional)
# HELMET_ENABLED=true

# Backup Configuration (Optional)
# BACKUP_ENABLED=true
# BACKUP_SCHEDULE=0 2 * * *  # Daily at 2 AM (cron format)
# BACKUP_PATH=/path/to/backups
